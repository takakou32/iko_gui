# ページ4の編集モードON時の編集可能テキストボックス - JSON要素参照レポート

## テキストボックス一覧表

| No. | テキストボックス名 | 表示行 | 参照元JSON要素 | 初期値読み込み | 保存機能 | 備考 |
|-----|-------------------|--------|---------------|--------------|---------|------|
| 1 | V1抽出CSV格納元 | 1行目のみ | `page4.json.SourcePath` | ❌ 未実装 | ❌ 未実装 | Load-PagePathsで読み込まれるが、ページ4ではv1CsvSourceTextBoxに設定されていない |
| 2 | KDL変換CSV格納元 | 1行目・2行目 | ❌ 存在しない | ❌ 未実装 | ❌ 未実装 | page4.jsonに該当要素なし |
| 3 | KDL変換CSV格納先 | 1行目・2行目 | ❌ 存在しない | ❌ 未実装 | ❌ 未実装 | page4.jsonに該当要素なし |
| 4 | V1抽出CSV格納先 | 全行 | ❌ 存在しない | ❌ 未実装 | ❌ 未実装 | page4.json.Processes[i].DestinationPathは存在するが、ページ4では未使用 |
| 5 | タスク名 | 全行 | `page4.json.Processes[i].Name` | ✅ 実装済み | ❌ 未実装 | ReadOnly = trueのため編集不可 |
| 6 | ログ格納先 | 共通セクション | `page4.json.LogStoragePath` | ✅ 実装済み | ✅ 実装済み | UILayout.ps1で実装済み |

## 実装状況サマリー

### ✅ 実装済み
- **タスク名**: 読み込みのみ（編集不可）
- **ログ格納先**: 読み込み・保存ともに実装済み

### ❌ 未実装
- **V1抽出CSV格納元**: 読み込み・保存ともに未実装
- **KDL変換CSV格納元**: 読み込み・保存ともに未実装（JSON要素も存在しない）
- **KDL変換CSV格納先**: 読み込み・保存ともに未実装（JSON要素も存在しない）
- **V1抽出CSV格納先**: 読み込み・保存ともに未実装（JSON要素も存在しない）

## 詳細説明

### 1. V1抽出CSV格納元テキストボックス
- **参照元**: `page4.json.SourcePath`
- **問題点**: 
  - `Load-PagePaths`関数で`SourcePath`は読み込まれますが、ページ4（currentPage == 3）では`v1CsvSourceTextBox`に設定されていません
  - ページ3（currentPage == 2）の場合のみ設定される実装になっています
  - クリック時に`Save-PagePaths -SourcePath`を呼び出していません

### 2. KDL変換CSV格納元テキストボックス
- **参照元**: 存在しない
- **問題点**: 
  - `page4.json`に該当するJSON要素が定義されていません
  - 初期値読み込み処理が未実装
  - 保存処理が未実装（クリック時にテキストボックスに設定するのみ）

### 3. KDL変換CSV格納先テキストボックス
- **参照元**: 存在しない
- **問題点**: 
  - `page4.json`に該当するJSON要素が定義されていません
  - 初期値読み込み処理が未実装
  - 保存処理が未実装（クリック時にテキストボックスに設定するのみ）

### 4. V1抽出CSV格納先テキストボックス
- **参照元**: 存在しない（`page4.json.Processes[i].DestinationPath`は存在するが未使用）
- **問題点**: 
  - ページ3では`Processes[i].DestinationPath`を使用していますが、ページ4では未使用
  - 初期値読み込み処理が未実装
  - 保存処理が未実装（クリック時にテキストボックスに設定するのみ）

### 5. タスク名テキストボックス
- **参照元**: `page4.json.Processes[i].Name`
- **実装状況**: 
  - 初期値読み込みは実装済み（1577行目）
  - `ReadOnly = $true`のため編集不可

### 6. ログ格納先テキストボックス
- **参照元**: `page4.json.LogStoragePath`
- **実装状況**: 
  - 初期値読み込みは実装済み（`Load-PagePaths`関数）
  - 保存機能は実装済み（`UILayout.ps1`で`Save-PagePaths -LogStoragePath`を呼び出し）

---
**レポート作成日**: 2026-01-09
**対象ファイル**: `Functions.ps1`, `page4.json`, `UILayout.ps1`
